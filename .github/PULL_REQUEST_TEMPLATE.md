## What

<!-- What changed and why. Link related issues if any. -->

## Checklist

- [ ] `pnpm build` passes
- [ ] `pnpm lint:check` passes
- [ ] `pnpm test:unit` passes at 100% coverage
- [ ] `src/core/` logic covered by mutation tests (`pnpm test:mutation`)
- [ ] `pnpm dep:cruise` passes (no layering violations)
